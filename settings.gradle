pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven {
            url = adaptive_uri
            credentials {
                username = adaptive_username
                password = adaptive_password
            }
        }
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven {
            url = adaptive_uri
            credentials {
                username = adaptive_username
                password = adaptive_password
            }
        }
    }
}

rootProject.name = "oneofftests"

